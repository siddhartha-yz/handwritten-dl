# Deep Learning from Scratch

From-scratch implementations of deep learning modules.

## Setup

Python 3.11, plus PyTorch (CPU), NumPy, pandas, and d2l.

```bash
uv python install 3.11
uv sync
source .venv/bin/activate
```

This machine does not currently expose an NVIDIA driver, so the default install uses CPU PyTorch.
